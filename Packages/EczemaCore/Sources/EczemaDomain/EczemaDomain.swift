// EczemaDomain — meals, skin observations and the rules that govern them.
//
// Deliberately empty. The schema and the domain models are on a spec deadline that
// has not closed: `docs/spec/persistence-model.md` carries the DATA-* rules, and
// jirigrill/eczema-helper#767 collects the owner confirmations still outstanding.
// Scaffolding is structure only — do not add models here ahead of that.
//
// This target has no dependencies, by design. It must stay buildable and testable
// without SwiftData, SwiftUI or CloudKit.
